import { Itim, M_PLUS_1,Roboto_Condensed } from 'next/font/google';

export const itim = Itim({subsets: ['latin'], weight:["400"] });
 export const mplus = M_PLUS_1({subsets: ['latin'], weight:["400","700"] });
 export const robo = Roboto_Condensed({ subsets: ['latin'], weight: '400' });

